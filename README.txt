┌─ Notes ──────────────────────────────────┐
│ you need a valid PRISM.rice to run       │
│ PRISM.                                   │
└──────────────────────────────────────────┘
┌─ Ports ──────────────────────────────────┐
│ No ports are currently being developed   │
│ zsh, bash and fish ports are planned.    │
└──────────────────────────────────────────┘
┌─ emberShell ─────────────────────────────┐
│ to install, run this command:            │
│ > esh /path/to/InstallPrism              │
└──────────────────────────────────────────┘
┌─ Nushell ────────────────────────────────┐
│ to install, add this to $nu.config-path: │
│ > source /path/to/NuPRISM.nu             │
└──────────────────────────────────────────┘
┌─ PowerShell ─────────────────────────────┐
│ to install, add this to $profile:        │
│ > . /path/to/PowerPRISM.ps1              │
└──────────────────────────────────────────┘
