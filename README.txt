┌─ Notes ───────────────────────────────┐
│ you need a valid PRISM.rice to run    │
│ PRISM.                                │
└───────────────────────────────────────┘
┌─ Ports ───────────────────────────────┐
│ a PowerShell port is in development   │
│ zsh, bash and fish ports are planned. │
└───────────────────────────────────────┘
┌─ emberShell ──────────────────────────┐
│ to install, run this command:         │
│ > esh /path/to/InstallPrism           │
└───────────────────────────────────────┘
┌─ Nushell ─────────────────────────────┐
│ to install, add this to config.nu:    │
│ > source /path/to/NuPRISM.nu          │
└───────────────────────────────────────┘
